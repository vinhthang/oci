apiVersion: apps/v1
kind: Deployment
metadata:
  name: umami
  labels:
    app: umami
spec:
  replicas: 1
  selector:
    matchLabels:
      app: umami
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: umami
    spec:
      nodeSelector:
        kubernetes.io/hostname: arm10
      containers:
        - name: umami
          image: ghcr.io/umami-software/umami:postgresql-v2.13.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: UMAMI_DATABASE_URL
            - name: DATABASE_TYPE
              value: "postgresql"
            - name: APP_SECRET
              value: "super_secret_umami_app_salt_2026_tokyo"
            - name: DISABLE_TELEMETRY
              value: "1"
          livenessProbe:
            httpGet:
              path: /api/heartbeat
              port: http
            initialDelaySeconds: 25
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/heartbeat
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 30m
              memory: 128Mi
            limits:
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: umami
spec:
  type: NodePort
  selector:
    app: umami
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30003
